# Alias for BCE baseline experiment
trainer:
  max_epochs: 10
  num_sanity_val_steps: 0
  limit_val_batches: null
  logger:
    init_args:
      name: tgn-baseline
      tags: [baseline, tgn, bce]

data:
  init_args:
    loss_mode: bce

model:
  init_args:
    loss_mode: bce
